{
  "name": "aft-project",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "build": "bun build index.ts --outdir ./dist",
    "test": "bun test",
    "fmt": "bun fmt",
    "seed:users": "bun scripts/seed-users.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
